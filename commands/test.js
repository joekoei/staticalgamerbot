const discord = require("discord.js");
const config = require("../botconfig.json");

module.exports.run => {
	
	var warns = require("../warns.json")

	fs.readdir(warns)

}


module.exports.help = {
    name: "test"
}
